# 物理动画视觉标准 v1

## 0. 标准身份与适用范围

- 本文是“动画制作”项目的视觉权威标准，适用于后续新建、改版和修复的全部物理动画页面。
- 本标准同时服务于教师视觉验收，以及 AI、设计和开发人员的直接实施。
- 默认必须遵守本文。题型确有特殊需要时，可以在不破坏视觉语言和可读性的前提下局部扩展；涉及颜色语义、标题结构、字号层级或响应式主布局的偏离，必须先取得教师确认。
- 本标准记录七项内容：颜色标准、标题与品牌结构、六级字号、三栏布局、窄屏与短横屏布局、CSS Token 和布局模板、正误示例。
- 人工逐页验收清单不属于本文 v1 的正式范围。

---

## 1. 颜色标准

### 1.1 基础界面色

| Token | 色值 | 用途 |
|---|---:|---|
| `--surface-page` | `#F7F4ED` | 页面和左右侧栏的暖白底色 |
| `--surface-panel` | `#F7F4ED` | 参数、观察面板背景 |
| `--surface-raised` | `#FFFDF8` | 卡片、浮层、次级按钮背景 |
| `--border` | `#D8D6CF` | 卡片边框、栏分隔线、输入控件边线 |
| `--text-strong` | `#14213D` | 标题、正文主文字、关键标签 |
| `--text-muted` | `#5C6678` | 辅助说明、次级标签、弱化信息 |
| `--action` | `#2557D6` | 主按钮、当前选项、焦点操作 |
| `--on-action` | `#FFFFFF` | 主操作色上的文字和图标 |

使用规则：

- 页面和侧栏保持暖白，避免改成大面积冷灰或纯黑。
- 卡片使用轻微层级差，不用重阴影制造层级。
- 主操作蓝只用于“当前可执行动作”和“选中状态”，不能成为大面积装饰色。
- 主文字与次文字必须保持明确层级，不能用浅灰承载重要结论。

### 1.2 状态色

| Token | 色值 | 用途 |
|---|---:|---|
| `--safe` | `#277A51` | 正确、完成、安全、满足条件 |
| `--safe-soft` | `#E5F4EB` | 成功状态浅底 |
| `--danger` | `#B9384D` | 错误、超限、危险、失败 |
| `--danger-soft` | `#FAE9EC` | 危险状态浅底 |
| `--warning` | `#8D5517` | 提醒、注意、尚未完成 |
| `--warning-soft` | `#FFF4E7` | 提醒状态浅底 |
| `--rising` | `#A84D19` | 增大、上升、趋强 |
| `--rising-soft` | `#FFF0E7` | 上升趋势浅底 |
| `--falling` | `#24569D` | 减小、下降、趋弱 |
| `--falling-soft` | `#EDF4FF` | 下降趋势浅底 |

使用规则：

- 状态色表达状态，不承担无意义装饰。
- 危险红不能用作普通主按钮。
- 上升和下降必须同时有文字、箭头或数值变化，不能只靠颜色传达。
- 浅底与对应深色文字成对使用。

### 1.3 方向与物理可视化色

| Token | 色值 | 默认语义 |
|---|---:|---|
| `--prograde` | `#E66D26` | 顺行、正向或热方向 |
| `--retrograde` | `#2557D6` | 逆行、反向或冷方向 |
| `--viz-circular` | `#38BDF8` | 圆轨道、主曲线、冷色对象 |
| `--viz-transfer` | `#FB923C` | 转移轨道、对比曲线、热色对象 |
| `--viz-reference` | `#55617C` | 参考轨迹、辅助线、非主对象 |
| `--viz-velocity` | `#4ADE80` | 速度或运动状态 |
| `--viz-gravity` | `#F6C453` | 重力、场力或关键受力 |
| `--viz-required` | `#C084FC` | 必需量、目标量、补充关系 |
| `--viz-apsis` | `#F8FAFC` | 关键位置、拱点、强调标记 |
| `--viz-escape` | `#FB7185` | 逃逸、超限、失稳路径 |

使用规则：

- 同一页面内，同一物理量必须始终使用同一种颜色。
- 不同物理量不要因为“好看”而临时交换颜色。
- 颜色必须与图例、标签或直接标注同时出现。
- Canvas、SVG、DOM 图例统一从 CSS Token 读取语义色，不在绘图代码里重复写散乱的十六进制颜色。
- 光谱、真实材料色、渐变介质和结构阴影可以使用题型专属颜色，但不能覆盖以上语义角色。

### 1.4 深色实验舞台

| Token | 色值 | 用途 |
|---|---:|---|
| `--stage` | `#0B1424` | 实验舞台和主要图表背景 |
| `--stage-line` | `#263650` | 舞台边线、工具栏边线、暗底网格 |
| `--stage-text` | `#F4F8FF` | 舞台主要文字 |
| `--stage-muted` | `#91A3BF` | 舞台辅助文字、次级刻度 |

舞台可以叠加轻微深蓝径向渐变，但基础底色必须保持 `--stage`。不使用纯黑背景。

---

## 2. 标题与品牌结构

### 2.1 固定结构

所有页面顶部使用统一结构：

```text
┌─────────────────────────────────────────────┐
│ 返回位   罗   老罗物理 · 探究实验            │
│              当前动画标题                    │
└─────────────────────────────────────────────┘
```

标准 HTML：

```html
<header class="topbar template-header">
  <div id="headerBackSlot"></div>
  <div class="brand">
    <div class="brand-mark" aria-hidden="true">罗</div>
    <div class="brand-copy">
      <p class="eyebrow">老罗物理 · 探究实验</p>
      <h1>当前动画标题</h1>
    </div>
  </div>
</header>
```

### 2.2 品牌规则

- 品牌字固定为“罗”，深蓝底、白字、粗体。
- 眉题固定为“老罗物理 · 探究实验”，不得替换为实验编号或版本信息。
- `#headerBackSlot` 始终保留，供网站嵌入返回按钮；独立页面中允许为空。
- 页面只设置一个可见 `h1`。
- 标题写教学主题，不写文件编号、版本号、开发状态或“高级重构版”等技术信息。
- 标题推荐格式为“主题｜学习任务”，例如“卫星变轨｜从圆轨道到新圆轨道”。
- 标题保持单行；空间不足时省略号截断，不把整个标题栏撑高。

### 2.3 标题栏尺寸

| 场景 | 标题栏高度 | “罗”标记 | 主标题 |
|---|---:|---:|---:|
| 宽屏 | `62px` | `38×38px` | `20px` |
| 窄屏 `≤960px` | `56px` | `34×34px` | `16px` |
| 短横屏 `≤960×540` | `46px` | `30×30px` | `14px` |

- 眉题始终为 `11px`，包括短横屏，不允许通过 `display:none` 隐藏。
- 主标题字重建议 `800`，行高 `1.15`。
- “罗”字重建议 `900`。
- 标题栏必须横跨页面顶部，不能在短横屏中被隐藏。

---

## 3. 六级字号

### 3.1 字体族

```css
--font-ui: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
  "PingFang SC", "Microsoft YaHei", sans-serif;
```

正文、按钮、输入框、选择器、Canvas 和 SVG 标注统一使用该字体栈。公式可以使用原生 MathML 或适当的数学字体。

### 3.2 字号层级

| 层级 | Token | 大小 | 主要用途 |
|---|---|---:|---|
| T1 | `--type-20` | `20px` | 宽屏页面主标题 |
| T2 | `--type-16` | `16px` | 窄屏主标题、极少数核心大读数 |
| T3 | `--type-14` | `14px` | 短横屏主标题、重要提示、核心读数、警报 |
| T4 | `--type-13` | `13px` | 正文、主要按钮、主要控制项 |
| T5 | `--type-12` | `12px` | 卡片标题、参数标签、数据、公式、表格 |
| T6 | `--type-11` | `11px` | 眉题、提示、图例、工具栏、辅助状态 |

### 3.3 字号规则

- 可见的 DOM、Canvas 和 SVG 文字不得低于 `11px`；原生 MathML 内部排版层级除外。
- 禁止使用 `8px`、`9px`、`10px`、`10.5px`、`13.5px`、`15px`、`18px`、`21px` 等临时字号。
- 禁止使用 `font-size:0` 隐藏文字后再用伪元素替换。
- 禁止使用整体 `transform:scale()` 缩小界面文字。
- 空间不足时优先缩短次要文案、减少刻度数量、折叠次级内容或允许局部滚动。
- 正文推荐行高 `1.5`；紧凑标签推荐 `1.35`；标题行高 `1.15`。
- 上下标不得因浏览器默认样式缩小到不可读，常规界面可设置 `sub, sup { font-size: 1em; }`。

---

## 4. 三栏布局

### 4.1 信息职责

```text
┌────────────────────── 顶部统一标题栏 ──────────────────────┐
├──── 左侧参数区 ────┬──────── 中间主舞台 ────────┬──── 右侧观察区 ────┤
│ 条件、参数、预设    │ Canvas / SVG / 核心现象    │ 数据、结论、图表    │
│ 约 220–280px       │ 弹性占满，视觉权重最大      │ 约 240–314px       │
└────────────────────┴───────────────────────────┴───────────────────┘
```

左侧参数区：

- 实验条件、可调参数、模式、预设、任务入口。
- 参数按教学顺序排列，每一步只突出一个主要操作焦点。
- 内容过多时只在左栏内部纵向滚动。

中间主舞台：

- 承载 Canvas、SVG、核心动画、关键物理量和舞台工具栏。
- 必须是三栏中最大区域，通常占可用内容宽度的 `50%–60%`。
- 同时高亮的核心物理信息默认不超过两组。
- 舞台控件属于舞台，不应散落进左右面板。

右侧观察区：

- 实时数据、图表、状态、结论、公式和观察提示。
- 不重复左侧参数，不放置无关的第二套控制系统。
- 内容过多时只在右栏内部纵向滚动。

### 4.2 栏宽标准

推荐 Grid：

```css
grid-template-columns:
  clamp(220px, 22vw, 280px)
  minmax(0, 1fr)
  clamp(240px, 21vw, 314px);
```

允许范围：

- 左栏：`220–280px`，推荐基准约 `260px`。
- 中栏：`minmax(0, 1fr)`，弹性占满剩余空间。
- 右栏：`240–314px`，推荐基准约 `280px`。
- 页面内边距：`8–12px`。
- 栏间距：`8–12px`。

布局规则：

- 中栏必须设置 `min-width:0`，Canvas/SVG 外层也必须允许收缩。
- 左右栏使用独立纵向滚动，页面本身不得出现横向滚动。
- Canvas 使用 `display:block;width:100%;height:100%`，并由脚本同步 backing store。
- 不得用固定超宽 Canvas、长公式或 `min-content` 把中栏撑出页面。
- 1024px 宽度仍优先保持三栏；必要时收紧两侧栏和间距，不缩小文字。

---

## 5. 窄屏与短横屏布局

### 5.1 窄屏 `≤960px`

结构顺序：

```text
标题栏
主实验舞台
参数设置 / 观察数据 两个标签
当前活动面板
```

规则：

- 不再同时展示左右两个侧栏，只展示一个活动面板。
- 两个标签固定为“参数设置”和“观察数据”。
- 切换面板不得重置动画、参数、时间、图表或观察状态。
- 活动面板独立纵向滚动。
- 舞台优先显示，不能被面板推到页面底部。
- 标签使用原生按钮并提供可访问的 tab/tabpanel 关系。

### 5.2 短横屏 `≤960px` 且高度 `≤540px`

```text
┌────────────────────── 标题栏 ──────────────────────┐
├──────────── 左侧主舞台 ───────────┬── 参数/观察标签 ──┤
│                                  ├──────────────────┤
│       约占内容宽度 60%–66%        │  单个活动面板     │
│                                  │  独立纵向滚动     │
└──────────────────────────────────┴──────────────────┘
```

规则：

- 标题栏保持显示，主标题为 `14px`，眉题仍为 `11px`。
- 左侧舞台约占 `60%–66%`，右侧区域约占 `34%–40%`。
- 推荐列定义：`minmax(0, 1.55fr) minmax(250px, .8fr)`。
- 右侧顶部放两个横向标签，下方放一个活动面板。
- 禁止竖排标签、隐藏标题、整体缩放页面或把字号降到 `11px` 以下。
- 舞台工具栏必须保持完整可操作；次要说明可以折叠或缩短。

### 5.3 面板状态要求

- 宽屏：左右面板同时可见，不显示移动端标签。
- 窄屏与短横屏：标签可见，仅一个侧栏可见且可访问。
- 返回宽屏时，左右面板重新同时显示，但保留当前实验状态。
- 标签至少支持点击、Enter、Space；推荐支持左右方向键、Home 和 End。

---

## 6. CSS Token 和布局模板

### 6.1 可直接复制的 Token

```css
:root {
  --font-ui: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
    "PingFang SC", "Microsoft YaHei", sans-serif;

  --type-20: 20px;
  --type-16: 16px;
  --type-14: 14px;
  --type-13: 13px;
  --type-12: 12px;
  --type-11: 11px;

  --surface-page: #f7f4ed;
  --surface-panel: #f7f4ed;
  --surface-raised: #fffdf8;
  --border: #d8d6cf;
  --text-strong: #14213d;
  --text-muted: #5c6678;
  --action: #2557d6;
  --on-action: #fff;

  --prograde: #e66d26;
  --retrograde: #2557d6;
  --safe: #277a51;
  --safe-soft: #e5f4eb;
  --danger: #b9384d;
  --danger-soft: #fae9ec;
  --warning: #8d5517;
  --warning-soft: #fff4e7;
  --rising: #a84d19;
  --rising-soft: #fff0e7;
  --falling: #24569d;
  --falling-soft: #edf4ff;

  --stage: #0b1424;
  --stage-line: #263650;
  --stage-text: #f4f8ff;
  --stage-muted: #91a3bf;

  --viz-circular: #38bdf8;
  --viz-transfer: #fb923c;
  --viz-reference: #55617c;
  --viz-velocity: #4ade80;
  --viz-gravity: #f6c453;
  --viz-required: #c084fc;
  --viz-apsis: #f8fafc;
  --viz-escape: #fb7185;
}
```

### 6.2 可直接复制的基础样式

```css
* { box-sizing: border-box; }

html,
body {
  margin: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: var(--surface-page);
  color: var(--text-strong);
  font-family: var(--font-ui);
  font-size: var(--type-13);
  -webkit-text-size-adjust: 100%;
}

button,
input,
select,
textarea {
  font-family: var(--font-ui);
  font-size: var(--type-13);
}

sub,
sup { font-size: 1em; }

input[type="range"] { min-height: 28px; }

canvas,
svg {
  display: block;
  max-width: 100%;
}
```

### 6.3 可直接复制的标题模板

```css
:root { --header-height: 62px; }

.template-header {
  height: var(--header-height);
  min-height: var(--header-height);
  padding: 0 18px;
  display: grid;
  grid-template-columns: 42px minmax(0, 1fr);
  align-items: center;
  gap: 12px;
  background: rgba(255, 253, 248, .96);
  border-bottom: 1px solid var(--border);
}

#headerBackSlot { width: 42px; height: 38px; }
.brand { min-width: 0; display: flex; align-items: center; gap: 11px; }
.brand-copy { min-width: 0; }

.brand-mark {
  width: 38px;
  height: 38px;
  flex: 0 0 38px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--text-strong);
  color: var(--on-action);
  font-size: 19px;
  font-weight: 900;
}

.eyebrow {
  margin: 0 0 2px;
  color: var(--text-muted);
  font-size: var(--type-11);
  font-weight: 800;
  line-height: 1.2;
  letter-spacing: .12em;
  white-space: nowrap;
}

.template-header h1 {
  margin: 0;
  overflow: hidden;
  color: var(--text-strong);
  font-size: var(--type-20);
  font-weight: 800;
  line-height: 1.15;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

### 6.4 可直接复制的三栏与响应式模板

```css
.experiment-layout {
  height: calc(100dvh - var(--header-height));
  min-width: 0;
  min-height: 0;
  display: grid;
  grid-template-columns:
    clamp(220px, 22vw, 280px)
    minmax(0, 1fr)
    clamp(240px, 21vw, 314px);
  grid-template-rows: minmax(0, 1fr);
  gap: 10px;
  padding: 10px;
  overflow: hidden;
}

.control-panel,
.observe-panel {
  min-width: 0;
  min-height: 0;
  overflow-y: auto;
  background: var(--surface-panel);
}

.stage {
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  background: var(--stage);
  border: 1px solid var(--stage-line);
}

.control-panel { grid-column: 1; }
.stage { grid-column: 2; }
.observe-panel { grid-column: 3; }
.mobile-tabs { display: none; }

@media (max-width: 960px) {
  :root { --header-height: 56px; }

  .template-header h1 { font-size: var(--type-16); }

  .experiment-layout {
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: minmax(350px, 56svh) 42px minmax(0, 1fr);
    gap: 6px;
    padding: 6px;
  }

  .stage { grid-column: 1; grid-row: 1; }
  .mobile-tabs {
    grid-column: 1;
    grid-row: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .control-panel,
  .observe-panel {
    grid-column: 1;
    grid-row: 3;
    display: none;
  }

  .experiment-layout[data-panel="conditions"] .control-panel,
  .experiment-layout[data-panel="observe"] .observe-panel {
    display: block;
  }
}

@media (orientation: landscape) and (max-width: 960px) and (max-height: 540px) {
  :root { --header-height: 46px; }

  .template-header h1 { font-size: var(--type-14); }

  .experiment-layout {
    grid-template-columns: minmax(0, 1.55fr) minmax(250px, .8fr);
    grid-template-rows: 42px minmax(0, 1fr);
  }

  .stage { grid-column: 1; grid-row: 1 / 3; }
  .mobile-tabs { grid-column: 2; grid-row: 1; }
  .control-panel,
  .observe-panel { grid-column: 2; grid-row: 2; }
}
```

模板中的类名可以按现有页面做兼容映射，但颜色、字号、信息职责和响应式行为不得改变。

---

## 7. 正误示例

### 7.1 颜色

正确：

- 主操作按钮统一使用 `--action`。
- 危险状态使用 `--danger` 加文字说明。
- Canvas 速度矢量和图例同时使用 `--viz-velocity`。

错误：

- 每个页面自行选择一个新的主蓝色。
- 用危险红制作普通播放按钮。
- 图中速度是绿色，图例却使用蓝色。

### 7.2 标题

正确：

- `老罗物理 · 探究实验`
- `卫星变轨｜从圆轨道到新圆轨道`

错误：

- `第01号实验：卫星变轨高级重构版 v5`
- 在 960×540 中隐藏标题栏或眉题。
- 为容纳长标题把主标题缩到 `11px`。

### 7.3 字号

正确：

- 正文 `13px`，数据标签 `12px`，辅助图例 `11px`。
- 空间不足时折叠次要说明或允许局部滚动。

错误：

- 临时使用 `9px` 或 `10.5px` 塞入更多内容。
- 使用 `transform:scale(.8)` 整体压缩文字。
- 使用 `font-size:0` 隐藏真实按钮文字。

### 7.4 三栏布局

正确：

- 左侧参数、中间舞台、右侧观察。
- 中间使用 `minmax(0, 1fr)`，左右栏独立滚动。
- 1024px 宽度仍保持舞台为最大区域。

错误：

- 让左右面板内容把 Canvas 挤出页面。
- 在右侧重复一整套左侧控制项。
- 页面出现横向滚动条。

### 7.5 窄屏与短横屏

正确：

- 390px：标题 → 舞台 → 标签 → 单活动面板。
- 960×540：标题在上，舞台在左，标签和活动面板在右。
- 切换参数/观察时保留动画状态。

错误：

- 窄屏同时堆叠两个完整侧栏。
- 短横屏隐藏标题或使用竖排标签。
- 切换面板时重置参数、时间或图表。

---

## 8. 标准维护规则

- 本文当前版本为 `v1`。
- 后续若只增加题型专属可视化色，不改变既有语义，可补充小版本说明。
- 修改基础色、品牌结构、六级字号、三栏职责或 `960px` 响应式边界，必须先取得教师确认并升级版本。
- 旧页面改版时优先通过最终适配层迁移，不因执行本标准而整体重写物理模型。
- 新页面从本文 CSS Token 和布局模板起步，不再自行建立另一套视觉变量。
